# Moved

The comparison track for branch `thevindu-models` lives next to `model-Thevindu/`, not
under `docs/`:

**[`thevindu-models/`](../../thevindu-models/)**

Runbooks, fixtures, scripts, and RESULTS are all in that folder.
