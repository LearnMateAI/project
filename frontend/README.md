For frontend, start by opening new terminal/shell window. When you are simulataneously engaed in development of frontend and backend, use multiple terminal/shell windows. 

```
cd <your-path-to-project-root-pls REPLACE>/frontend
```

To starting Running frontend server:

```
npm run dev
```

To stop running server:
```
ctrl + C  (windows)
```