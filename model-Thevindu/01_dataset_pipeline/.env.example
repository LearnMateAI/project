# Copy to .env and fill in. Never commit .env.
# Rotate any key that was ever pasted into chat, a notebook cell, or a log.
# Load via python-dotenv only — do not pass the key as a CLI argument.
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini
# Optional aliases used by older scripts:
# LM_API_KEY=
# LM_API_BASE=
# LM_MODEL=
