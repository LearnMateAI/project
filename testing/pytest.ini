[pytest]
testpaths = testing
pythonpath = integrated-backend
addopts = -q --strict-markers
markers =
    uat: live stack user-acceptance tests (skipped unless LEARNMATE_UAT=1)
filterwarnings =
    ignore::DeprecationWarning
